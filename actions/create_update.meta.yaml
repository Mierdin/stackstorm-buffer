---
  name: "create_update"
  runner_type: "python-script"
  description: "Share something on buffer"
  enabled: true
  entry_point: "create_update.py"
  parameters:
    post_prefix:
      type: "string"
      description: "Post prefix for the thing you're sharing"
      required: true
    title:
      type: "string"
      description: "Title of the thing you're sharing"
      required: true
    url:
      type: "string"
      description: "URL of the thing you're sharing"
      required: true
    post_image:
      type: "string"
      description: "URL of the image for the thing you're sharing"
      required: false
